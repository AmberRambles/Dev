const kelvin = 0; //creates var kelvin and assigns value of 293
const celsius = kelvin - 273;
//creates celsius var and applies the conversion factor
let fahrenheit = celsius*(9/5) + 32;
//creates fahrenheit var and applies conversion on celsius
fahrenheit = Math. floor(fahrenheit);
/rounds the value of fahrenheit down to the nearest integer console. log ('The temperature is ${fahrenheit} degrees Fahrenheit') ;
let newton = celsius* (33/100);
/create newton variable and apply conversion factor
newton = Math. floor (newton);
/round down answer
console. log ('The temperature is ${newton} degrees Newton' ) ;